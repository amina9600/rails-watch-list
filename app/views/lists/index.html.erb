<h1>Movie Lists</h1>

<h3>My Lists</h3>

<%= link_to 'Create a new movie list', new_list_path %>

<% @list.each do |list|%>
  <%= link_to list.name, list_path(list) %>
  <%= link_to 'Delete', list_path(list), data: { turbo_method: :delete } %>
<% end %>
